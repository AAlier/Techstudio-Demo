file("../credentials.properties").with { propFile ->
    if (propFile.canRead()) {
        def properties = new Properties()
        properties.load(new FileInputStream(propFile))
        ext.API_KEY = properties['apiKey']
    } else {
        println 'Unable to read credentials.properties, using defaults'
        ext.API_KEY = "qwTLyL0N5KsDTFUwPc79MPPas0Oay3rv"
    }
}
